const POSITION_DEFAULTS = {
  x: 0,
  y: 0,
  z: 0,
} as const

const QUATERNION_DEFAULTS = {
  x: 0,
  y: 0,
  z: 0,
  w: 1,
} as const

const SCALE_DEFAULTS = {
  x: 1,
  y: 1,
  z: 1,
} as const

export {
  POSITION_DEFAULTS,
  QUATERNION_DEFAULTS,
  SCALE_DEFAULTS,
}
