// eslint-disable-next-line no-restricted-imports
import type {
  Texture, Material, Mesh, PlaneGeometry, Plane, Color, Vector2,
  Object3D,
} from 'three'

export type {
  Texture, Material, Mesh, PlaneGeometry, Plane, Color, Vector2,
  Object3D,
}
