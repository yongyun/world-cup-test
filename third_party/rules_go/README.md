# Rules Go patch

This patch is used to correctly set the external folder path when rules go is used
to compile c++ code. This fixes issues with some of our toolchain scripts, which
rely on files generated in external/

(Credit: @filippotrimoldi)