diff --color -ruN tensorflow/lite/delegates/gpu/metal/common.mm tensorflow/lite/delegates/gpu/metal/common.mm
--- tensorflow/lite/delegates/gpu/metal/common.mm	2023-09-27 16:49:33
+++ tensorflow/lite/delegates/gpu/metal/common.mm	2024-06-27 21:32:15
@@ -139,19 +139,6 @@
   } else if (@available(macOS 10.11, iOS 9.0, tvOS 9.0, *)) {
     [options setLanguageVersion:MTLLanguageVersion1_1];
   }
-#if (defined(__MAC_10_11) && __MAC_OS_X_VERSION_MIN_REQUIRED >= __MAC_10_11) ||    \
-    (defined(__IPHONE_9_0) && __IPHONE_OS_VERSION_MIN_REQUIRED >= __IPHONE_9_0) || \
-    (defined(__TVOS_9_0) && __TV_OS_VERSION_MIN_REQUIRED >= __TVOS_9_0)
-  // Minimum target OS version is able to support Metal.
-#else
-#pragma message(VAR_NAME_VALUE(__MAC_OS_X_VERSION_MIN_REQUIRED))
-#pragma message(VAR_NAME_VALUE(__IPHONE_OS_VERSION_MIN_REQUIRED))
-#pragma message(VAR_NAME_VALUE(__TV_OS_VERSION_MIN_REQUIRED))
-// NOLINTBEGIN
-#error \
-    "The Metal delegate is not supported on current target SDK. Minimum supported os: iOS/tvOS 9.0, macOS 10.11"
-// NOLINTEND
-#endif
 
   NSMutableDictionary<NSString*, NSString*>* macros_dict = [NSMutableDictionary dictionary];
   for (const auto& pair : macros) {
diff --color -ruN tensorflow/lite/ios/ios.bzl tensorflow/lite/ios/ios.bzl
--- tensorflow/lite/ios/ios.bzl	2023-09-27 16:49:33
+++ tensorflow/lite/ios/ios.bzl	2024-06-27 15:29:02
@@ -6,7 +6,7 @@
 load("@build_bazel_rules_apple//apple:ios.bzl", "ios_static_framework")
 
 # LINT.IfChange
-TFL_MINIMUM_OS_VERSION = "11.0"
+TFL_MINIMUM_OS_VERSION = "13.0"
 # LINT.ThenChange(
 #   TensorFlowLiteC.podspec.template,
 #   TensorFlowLiteSelectTfOps.podspec.template,
diff --color -ruN third_party/pthreadpool.patch third_party/pthreadpool.patch
--- third_party/pthreadpool.patch	2023-09-27 16:49:33
+++ third_party/pthreadpool.patch	2024-06-27 19:25:52
@@ -1,14 +1,13 @@
-diff --git a/BUILD.bazel b/BUILD.bazel
-index 40c5b9f..34d9eb0 100644
---- a/BUILD.bazel
-+++ b/BUILD.bazel
+diff --color -ruN a/BUILD.bazel b/BUILD.bazel
+--- a/BUILD.bazel	2021-03-03 11:45:46
++++ b/BUILD.bazel	2024-06-27 19:25:23
 @@ -1,4 +1,5 @@
  load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_test")
 +load("@bazel_skylib//lib:selects.bzl", "selects")
  
  licenses(["notice"])
  
-@@ -332,26 +333,25 @@ config_setting(
+@@ -332,26 +333,25 @@
  
  config_setting(
      name = "ios",
@@ -46,7 +45,7 @@
  )
  
  config_setting(
-@@ -404,14 +404,14 @@ config_setting(
+@@ -404,14 +404,14 @@
  config_setting(
      name = "emscripten",
      values = {
@@ -63,6 +62,18 @@
 +        "cpu": "wasm32",
 +        "features": "pthread",
      }
--)
-+)
-\ No newline at end of file
+ )
+diff --color -ruN a/src/gcd.c b/src/gcd.c
+--- a/src/gcd.c	2021-03-03 11:45:46
++++ b/src/gcd.c	2024-06-27 19:20:36
+@@ -21,6 +21,10 @@
+ #include "threadpool-object.h"
+ #include "threadpool-utils.h"
+ 
++#ifndef DISPATCH_APPLY_AUTO
++#define DISPATCH_APPLY_AUTO dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0)
++#endif
++
+ static void thread_main(void* arg, size_t thread_index) {
+ 	struct pthreadpool* threadpool = (struct pthreadpool*) arg;
+ 	struct thread_info* thread = &threadpool->threads[thread_index];
