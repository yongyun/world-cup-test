{
  "name": "http-file-server",
  "description": "An http server that can serve local files, particularly files built by bazel.",
  "private": true,
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "qrcode-terminal": "^0.12.0",
    "selfsigned": "^2.0.1"
  }
}
